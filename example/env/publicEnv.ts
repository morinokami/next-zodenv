import { zenv } from 'next-zodenv'
import { z } from 'zod'

// export const publicEnv = zenv(
//   z.object({
//     BAR: z.string(),
//   }),
//   { nextPublic: true },
// )

export const publicEnv = {}
